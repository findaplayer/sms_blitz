use Mix.Config

config :sms_blitz, :plivo, {"User ID", "User Token"}
config :sms_blitz, :itagg, {"Username", "Password", "Route"}
config :sms_blitz, :twilio, {"AccountSid", "authtoken"}
config :sms_blitz, :nexmo, {"Key", "Secret"}
